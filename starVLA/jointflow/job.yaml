REQUIRED:
  JOB_NAME: "starvla-libero-8gpu"
  JOB_PASSWD: "newk8s666"

  UPLOAD_DIR: "/home/users/sen02.wang-labs/aidi_jobs/starvla_dev"
  PROJECT_ID: "horizon-labs"

  WORKER_MIN_NUM: 1
  WORKER_MAX_NUM: 1
  GPU_PER_WORKER: 8

  # UPLOAD_DIR 是 starvla_dev，代码仓在 starvla_dev/starVLA；
  # 仓库内入口脚本路径固定为 starVLA/jointflow/run_aidi.sh。
  RUN_SCRIPTS: "${WORKING_PATH}/starVLA/starVLA/jointflow/run_aidi.sh"

OPTIONAL:
  FRAMEWORK: "pytorch"
  TASK_LABEL: "DEV"
  PRIORITY: 5
  DOCKER_IMAGE: "docker.hobot.cc/imagesys/starvla:v1.0"
  WALL_TIME: 20000
  JOB_TYPE: "train"
  REMARK: "starvla libero single-node 8gpu"
  DATA_SPACE:
    DATA_TYPE: "dmp"
    INPUT: "robot_lab"
    OUTPUT: "robot_lab"
