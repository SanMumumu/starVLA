#######
# RoboDojo evaluation policy-server job: one H20 node, eight StarVLA servers.
# Open the job terminal and run section 4 of run.sh after submission.
#######
REQUIRED:
  JOB_NAME: "starvla-robodojo-policy-server-8gpu"
  JOB_PASSWD: "newk8s666"
  UPLOAD_DIR: "/home/users/sen02.wang/workspace/starvla_dev"
  PROJECT_ID: "horizon-labs"
  WORKER_MIN_NUM: 1
  WORKER_MAX_NUM: 1
  GPU_PER_WORKER: 8
  RUN_SCRIPTS: "${WORKING_PATH}/run_aidi.sh"

OPTIONAL:
  FRAMEWORK: "pytorch"
  TASK_LABEL: "DEV"
  PRIORITY: 5
  DOCKER_IMAGE: "docker.hobot.cc/imagesys/starvla:v2.1"
  WALL_TIME: 20000
  JOB_TYPE: "train"
  REMARK: "RoboDojo inference server; one H20 node; eight StarVLA policy-server processes; ports 7777-7784"
  DATA_SPACE:
    DATA_TYPE: "dmp"
    INPUT: "robot_lab"
    OUTPUT: "robot_lab"
