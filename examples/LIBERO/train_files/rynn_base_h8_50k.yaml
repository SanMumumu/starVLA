# LIBERO RynnBrain1.1 causal-DINO MoT recipe.
# Model ABI stays Rynn (dual-view 512x256 composite, MoT, DINO-B).
# Non-model train/data controls follow WAM exp5 (0620_wam_exp5_2b_bigbs_policy,
# LIBERO-plus ~81.7%): no proprio state, 60K steps, warmup 3K, action LR 5e-5,
# unfrozen VLM, logging 100. Text supervision stays off (LIBERO-plus needs no
# subtask labels). Global batch stays 256 on the maintained 16-GPU topology
# (WAM yaml used accum=8; that is not copied here to avoid OOM on Rynn MoT).

run_id: starvla_rynnbrain11_libero_dualview_dino_mot_base_h8_bs256_60k_nostate_wamhyper_v1
run_root_dir: /horizon-bucket/robot_lab/users/sen.wang-labs/starVLA/outputs/starvla_libero
seed: 42
wandb_entity: axi-the-cat
wandb_project: starVLA_Libero
is_debug: false
version_id: "0.21"

framework:
  name: QwenWorldActionMoT
  enable_world_action_mot: true

  qwenvl:
    base_vlm: /horizon-bucket/robot_lab/users/sen.wang-labs/starVLA/CKPTS/rynnbrain1.1-2B/
    attn_implementation: sdpa
    require_attn_implementation: true
    enable_gradient_checkpointing: true
    enable_thinking: false
    vl_hidden_dim: 2048
    truncate_vlm_layers: 0

  planner:
    num_world_queries: 16
    num_action_queries: 8
    world_placeholder_token: "<WORLD_PLAN>"
    action_placeholder_token: "<ACTION_PLAN>"
    text_supervision:
      enabled: false

  world_action_mot:
    architecture: causal_dino_mot
    interaction_mode: base
    world_attention_mask_mode: first_frame_causal
    world_hidden_size: 512
    action_hidden_size: 1024
    world_ffn_dim: 2048
    action_ffn_dim: 4096
    num_layers: 30
    num_attention_heads: 24
    attention_head_dim: 128
    layerwise_planner_coupling: false
    norm_eps: 1.0e-06
    time_frequency_dim: 256
    # The 256x512 composite produces a 16x32 DINO grid; pool=2 -> 8x16.
    world_grid_height: 8
    world_grid_width: 16
    max_world_tokens: 128
    num_world_views: 1
    enable_gradient_checkpointing: true
    world_train_shift: 5.0
    world_infer_shift: 5.0
    world_num_train_timesteps: 1000
    action_train_shift: 5.0
    action_infer_shift: 5.0
    action_num_train_timesteps: 1000
    num_inference_timesteps: 20
    action_prediction_type: velocity
    action_velocity_target: noise_minus_clean
    jit_t_eps: 0.05
    repeated_diffusion_steps: 1
    action_loss_weight: 1.0
    world_loss_weight: 1.0
    text_loss_weight: 0.0

  dino:
    name: dinov3_vitb16
    model_size: base
    hf_model_id: /horizon-bucket/robot_lab/users/sen.wang-labs/starVLA/CKPTS/DINO-B/
    repo_or_dir: facebookresearch/dinov3
    weights: /horizon-bucket/robot_lab/users/sen.wang-labs/starVLA/CKPTS/DINO-B/
    loader: hf
    image_size: [256, 512]
    patch_size: 16
    embed_dim: 768
    stats_path: null
    load_live_backbone: true
    force_online: true
    dino_pool: 2
    current_dino_pool: null

  action_model:
    action_dim: 7
    # MoT contract: nostate training requires state_dim=0 (WAM exp5 used DiT, not MoT).
    state_dim: 0
    action_horizon: 8

datasets:
  vla_data:
    dataset_py: jointflow
    data_root_dir: /horizon-bucket/robot_lab/users/sen.wang-labs/starVLA/DATA/LEBERO/libero
    data_mix: libero_all
    image_layout: libero_dual_view_composite
    composite_source_view_keys:
      - video.primary_image
      - video.wrist_image
    composite_view_key: video.libero_dual_view_composite
    # WAM exp5 / LIBERO-plus eval path: policy is image+language only.
    include_state: false
    action_type: delta_qpos
    # LIBERO LeRobot files already store delta-qpos labels; do not delta them twice.
    action_mode: abs
    action_horizon: 8
    world_model:
      future_stride: 8
    future_valid_requires_full_stride: true
    decode_future_video: true
    online_dino: true
    dino_target_latents: false
    text_annotations:
      enabled: false
    sequential_step_sampling: false
    CoT_prompt: "Your task is {instruction}. To identify the key objects for your task. Locate their bounding boxes in [x1,y1,x2,y2] format."
    balance_dataset_weights: false
    balance_trajectory_weights: false
    per_device_batch_size: 16
    load_all_data_for_training: true
    obs_image_size: [512, 256]
    video_backend: torchvision_av
    num_workers: 16
    prefetch_factor: 4
    pin_memory: true
    persistent_workers: true

trainer:
  seed_before_model_init: true
  # 16 micro-batch x 16 GPUs x 1 accumulation step = global 256.
  expected_global_batch_size: 256
  max_train_steps: 60000
  num_warmup_steps: 3000
  save_interval: 5000
  eval_interval: 100000
  world_validation:
    enabled: false
  pretrained_checkpoint: null
  is_resume: false
  save_full_training_state: true
  learning_rate:
    base: 2.5e-05
    qwen_vl_interface: 1.0e-05
    action_plan_queries: 1.0e-04
    world_plan_queries: 1.0e-04
    action_model: 5.0e-05
  lr_scheduler_type: cosine_with_min_lr
  scheduler_specific_kwargs:
    min_lr: 1.0e-06
  # WAM exp5 effective freeze list is empty (boolean true in yaml is ignored).
  freeze_modules: ""
  loss_scale:
    vla: 1.0
  max_grad_norm: 1.0
  weight_decay: 0.0
  logging_frequency: 100
  log_grad_norms: false
  log_global_grad_norm: true
  deepspeed_skip_unused_param_anchors: true
  gradient_clipping: 1.0
  gradient_accumulation_steps: 1
  gradient_checkpointing: true
  optimizer:
    name: AdamW
    betas: [0.9, 0.95]
    eps: 1.0e-08
    weight_decay: 1.0e-08
